import React from 'react';
import Layout from '../layout';
import Login from './Login';

const App: React.FC = () => {
    return (
        <Layout>
            <AuthPage />
        </Layout>
    );
};

export default App;
